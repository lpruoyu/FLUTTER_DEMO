import 'package:flutter/material.dart';

class TBHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Container(
        child: Center(
          child: Text("首页"),
        ),
      );
}

class TBSettingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Container(
    child: Center(
      child: Text("设置"),
    ),
  );
}
